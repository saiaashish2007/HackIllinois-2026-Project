# Copy this file to .streamlit/secrets.toml and fill in your keys.
# NEVER commit secrets.toml to Git.

# OpenAI — powers the LLM strategy parser & detailed reports
# The app works without this (falls back to rule-based parsing & template reports)
OPENAI_API_KEY = "sk-..."
